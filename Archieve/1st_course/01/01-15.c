#include <stdio.h>
#include <math.h>

const double EPS = 10e-6;

int main(void)
	{
		double a, b, c, d;
		scanf("%lf%lf%lf%lf", &a, &b, &c, &d);
		if (sqrt(a*a + b*b) + EPS <= sqrt(1/2.0)*
	}